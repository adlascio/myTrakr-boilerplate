$(() => {
  
    const categorySelect = $("#categorySelect");
    const categoryInput = $("#categoryInput");
    const newCategoryButton = $("#categoryBtn");
    categoryInput.hide();
    newCategoryButton.hide();

});
